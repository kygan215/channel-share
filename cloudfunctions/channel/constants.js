// ============================================================
// 常量 & 共享实例
// ============================================================
// 这个文件是所有云函数模块的"公共插座"。
// 其他模块都从这个文件获取：
//   数据库连接（db）
//   查询操作符（_）
//   配置文件（SETUP_TOKEN、SENSITIVE_WORDS）
//   权限配置（publicActions、actionPermissions）
// ============================================================

// 微信云函数开发工具包
const cloud = require('wx-server-sdk');
// 初始化云服务（自动使用当前云环境）
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

// 数据库实例 — 所有数据操作都用这个
const db = cloud.database();

// 查询操作符 — 用于高级查询，比如：
//   _.in([1,2,3])  → 字段值在数组内
//   _.gte(100)     → 字段值 >= 100
const _ = db.command;

// Excel 处理库（SheetJS）
const XLSX = require('xlsx');

// ============================================================
// 配置常量
// ============================================================

// 管理员初始化令牌（上线前请修改为随机字符串！）
// 这个令牌用于 authInit 接口，防止任何人随便注册成管理员
const SETUP_TOKEN = 'CHANNEL_ADMIN_2026';

// 评论敏感词列表
// addComment 时逐词检测，含有这些词的评论会被拒绝
const SENSITIVE_WORDS = [
  'fuck', 'FUCK',    // 英文脏话
  '反动',            // 政治敏感
  '色情',            // 色情内容
  '赌博',            // 赌博
  '毒品',            // 毒品
  '枪支',            // 枪支
  '暴力',            // 暴力
  '诈骗',            // 诈骗
  '传销',            // 传销
  '法轮功',          // 邪教
];

// ============================================================
// 权限配置
// ============================================================

// 无需身份校验的公开操作列表
// 这些操作任何人都能调用（不需要登录）
const publicActions = [
  'whitelistCheck',   // 验证手机号是否在白名单
  'authCheck',        // 查询自己的身份
  'authInit',         // 用令牌注册管理员
  'authAddUser',      // 管理员添加用户
  'authRemoveUser',   // 管理员删除用户
  'authListUsers',    // 管理员查看所有用户
];

// 各操作允许的角色
// 没有列出的操作默认只允许 admin
const actionPermissions = {
  getRecord:        ['admin', 'viewer'],  // 查看详情：管理员和查看者都可以
  exportExcel:      ['admin'],            // 导出Excel：仅管理员
  batchAdd:         ['admin'],            // 批量导入：仅管理员
  batchDelete:      ['admin'],            // 批量删除：仅管理员
  healthCertReview: ['admin'],            // 健康证审核：仅管理员
  deleteAccount:    ['admin'],            // 注销账号：仅管理员
};

// ============================================================
// 导出
// ============================================================
module.exports = {
  cloud,      // 云服务实例
  db,         // 数据库实例
  _,          // 查询操作符
  XLSX,       // Excel 处理库
  SETUP_TOKEN,              // 管理员注册密码
  SENSITIVE_WORDS,          // 敏感词列表
  publicActions,            // 公开操作列表
  actionPermissions,        // 各操作的角色权限
};
